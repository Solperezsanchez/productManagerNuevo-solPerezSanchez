<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyecto Mew - Inicio</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: 'Fonseca', sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        header {
            background-color: #8e44ad;
            color: #fff;
            text-align: center;
            padding: 20px 0;
        }
        .container {
            max-width: 1200px;
            margin: 20px auto;
            padding: 0 20px;
        }
        h1 {
            font-size: 2rem;
            margin-bottom: 20px;
        }
        .product-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
        }
        .product-card {
            background-color: #fff;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .product-title {
            font-size: 1.2rem;
            color: #333;
            margin-bottom: 10px;
        }
        .product-description {
            font-size: 1rem;
            color: #666;
            margin-bottom: 10px;
        }
        .product-price {
            font-size: 1.2rem;
            color: #8e44ad;
            margin-bottom: 10px;
        }
        .product-thumbnail {
            width: 100%;
            border-radius: 8px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <header>
        <img src="../img/logo Proyecto mew.png" alt="Proyecto Mew">
    </header>
    <div class="container">
        <h1>Lista de Productos</h1>
        <div class="product-list">
            {{#each products}}
            <div class="product-card">
                <img class="product-thumbnail" src="{{this.thumbnail}}" alt="{{this.title}}">
                <h2 class="product-title">{{this.title}}</h2>
                <p class="product-description">{{this.description}}</p>
                <p class="product-price">$ {{this.price}}</p>
            </div>
            {{/each}}
        </div>
    </div>
</body>
</html>
